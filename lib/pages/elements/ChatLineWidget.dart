import 'package:flutter/widgets.dart';

class ChatLineWidget extends StatelessWidget {
  const ChatLineWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
